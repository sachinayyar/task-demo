pipeline {
    agent {
        kubernetes{
            label 'java21'
            defaultContainer 'jnlp'
        }
    }
    stages {
        stage('checkout') {
            steps {
                checkout scm
            }
        }        
        stage('Maven Build') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}
