apiVersion: v1
kind: Service
metadata:
  name: nexus-service
spec:
  type: NodePort
  selector:
    app: nexus
  ports:
    - protocol: TCP
      port: 8081            # external service port
      targetPort: 8081      # container port
      nodePort: 30222       # optional, or leave it to auto-assign
