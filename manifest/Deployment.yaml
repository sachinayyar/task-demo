apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-app
  namespace: test-uat
  labels:
    app: spring-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-app
  template:
    metadata:
      labels:
        app: spring-app
    spec:
      imagePullSecrets:
      - name: dockerhub-secret2    
      containers:
        - name: spring-app
          image: ayyarsachin/simpleproj-ms-stage:12
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
